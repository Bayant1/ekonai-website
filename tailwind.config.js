module.exports = {
    darkMode: 'class', // Enable dark mode using the 'class' strategy
    content: ['./**/*.html', './**/*.js'], // Ensure Tailwind scans your files for classes
    theme: {
      extend: {},
    },
    plugins: [],
  };